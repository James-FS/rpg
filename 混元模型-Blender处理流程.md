# 混元 3D 模型 → Mixamo → Unity 处理流程

> 本项目角色/道具资产的标准制作流程。由 AI 助手通过 Blender MCP 自动执行大部分步骤，
> 人工只负责混元下载、Mixamo 上传绑骨、Unity 最终验证。

---

## 一、混元下载设置

| 项目 | 角色 | 小道具（草药、矿灯等） |
|---|---|---|
| **格式** | GLB | GLB |
| **面数** | 500k | 50k |

- **只下 GLB**。FBX 会导致 Mixamo 报 `unable to map your existing skeleton`（混元 FBX 自带非标准节点结构）；OBJ 贴图网格分离易丢件。
- 生成提示词要点见文末附录。

---

## 二、Blender 处理流程（AI 通过 MCP 执行）

以 `药师.glb` 为例，共 7 步：

### 1. 清空场景，导入 GLB

`文件 → 导入 → glTF 2.0`。导入后体检：网格数量、面数、贴图齐全度、有无骨架/多余对象。
预期结果：1 个网格、约 50 万面（500k 档）、3 张 PBR 贴图（basecolor / metallic-roughness / normal）、无骨架。

### 2. 扶正

**混元 GLB 的模型全部是躺着的**（身高在 Y 轴、Z 轴是厚度）。
判断方法：站立的模型 Z 轴（高度）应为最大值。躺着的绕 X 轴旋转 +90° 立起来。
不扶正 = Mixamo 里角色躺平（本项目实际踩过的坑）。

### 3. 调整身高到真实比例

| 角色 | 目标身高 |
|---|---|
| 主角（男性冒险者） | 1.70 m |
| 药师林洛（女性） | 1.65 m |
| 铁匠阿拓（敦实男性） | 1.72 m |

缩放系数 = 目标身高 ÷ 当前身高，三轴统一缩放。

> ⚠️ 坑：Blender 面板尺寸显示可能不实时刷新，按旧值算会出巨人。
> 稳妥做法：旋转后的尺寸用「原始各轴 × 推算」手工计算，缩放后再读一次 dimensions 验证。

### 4. 应用全部变换（Apply All Transforms）

把旋转、缩放固化进网格数据（旋转归零、缩放归一）。
不做这步，导出的文件带隐藏变换，Mixamo/Unity 里姿势会歪。

### 5. 减面到 9 万面左右

加 **Decimate（精简）修改器**：比例 = 90000 ÷ 当前面数，应用。
500k → 90k 是 5 倍压缩，形态无损；1.5m → 90k 是 16 倍压缩，会有细节损失
（主角就是这么做的，贴图接缝处有轻微拉伸，因此后续角色统一选 500k 档）。

### 6. 清理悬浮碎屑

混元模型周围常有一批生成噪点碎块。做法：连通块（BMesh 岛屿）分析，
**删除顶点数 < 50 的孤岛**。典型删除量：1.8 万～2 万顶点。
剩下的几百个较大碎块（头发丝、衣片的结构性分块）视觉完整，保留。

### 7. 视口目检 + 导出三件套

导出前把镜头对准模型截图确认：姿势对、贴图对、无破损。然后导出到下载文件夹：

| 文件 | 用途 | 关键导出设置 |
|---|---|---|
| `英文前缀_mixamo.obj` | 上传 Mixamo | 只选模型网格；**Forward = -Z，Up = Y**（Y 轴朝上）；不带材质；文件名用英文 |
| `英文前缀_basecolor.png` | Unity 颜色贴图 | 从材质节点直接另存 |
| `英文前缀_normal.png` | Unity 法线贴图（可选） | 同上；挂到 Unity 材质 Normal Map 槽前需把贴图导入类型改为 Normal map |

> ⚠️ 坑：OBJ 导出若不显式设 Y-up，Mixamo 里模型就是躺的（本项目实际踩过的坑）。

---

## 三、Mixamo 绑骨（人工操作）

1. 上传 `xxx_mixamo.obj`
2. 放 5 个标记点：**下巴、左手腕、右手腕、左肘、右肘**
3. 下载角色本体（一次）：**FBX Binary + T-Pose + With Skin**
4. 下载动画（每个一次）：**FBX Binary + Without Skin + 30 FPS + Keyframe Reduction: none**
   - With Skin 只用于角色本体；动画一律 Without Skin，否则每个动画文件都重复打包整个角色
   - 微调参数（Overdrive / Trim / Sway / Breathing / Object Weight）全部保持默认，
     等进 Unity 看过实际效果再针对性回来调

已绑骨失败时的备选：AccuRIG（对非标准模型容忍度更高）或让我合并碎壳后重导。

---

## 四、Unity 导入（AI 执行）

- 角色 FBX + 贴图复制到 `rpg/Assets/Models/Character/`（NPC 可建子文件夹）
- Rig 设为 **Humanoid**（`.meta` 中 `animationType: 3`）
- 动画：Idle/Walk 等循环动画勾 **Loop Time**；一次性动作（采集等）不勾
- 材质：URP Lit + basecolor 贴图挂 Base Map
- Animator：主角复用 `PlayerAnimatorDriver` 的 `speed`（0~1）驱动
  Idle/Walk/Run 混合树；NPC 另建共用 Controller

---

## 五、本项目已完成的角色

| 角色 | 文件 | 状态 |
|---|---|---|
| 主角（男性冒险者） | `Character_Rigged.fbx` | ✅ 已进项目，Humanoid，9 万面，1.70 m |
| 药师林洛 | `herbalist_mixamo.obj` | 🔄 待 Mixamo 绑骨 |
| 铁匠阿拓 | `blacksmith_mixamo.obj` | 🔄 待 Mixamo 绑骨 |
| 月光药草 / 蓝色矿灯 | — | ⬜ 道具，无需绑骨，生成后直接进 Blender → Unity |
| 阿萤（小女孩） | — | ⬜ 待生成（提示词见剧本讨论记录） |

---

## 六、铁律总结（三条坑）

1. **混元 GLB 默认躺着** —— 必扶正（绕 X 轴 +90°）
2. **轴向必须显式设 Y-up** —— 否则 Mixamo/Unity 里躺平
3. **Blender 面板尺寸可能不刷新** —— 缩放计算后必须二次验证

---

## 附录：提示词模板

**角色（人类 NPC）**：

> 全身〔年龄性别身份〕游戏角色，站立T-pose双臂微张面向前方，中世纪低多边形半写实，
> 〔服装描述〕，〔体型气质〕，比例正常手脚与身体相连，
> 无披风无头盔无面具无武器无盾牌无背包无翅膀无尾巴，单人模型无地面背景，适合Unity自动绑骨

注意：女性角色必须写明"女性"；长裙必须写"裙摆及膝双腿可见"（防糊腿绑骨失败）；
武器一律不要（后续 Unity 单独挂）。

**道具**：

> 一〔道具描述〕游戏道具，低多边形半写实，〔造型/颜色/发光描述〕，
> 单个道具无背景无人物，风格干净适合游戏场景摆放
